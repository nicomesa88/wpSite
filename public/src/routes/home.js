import page__links from '../theme/page__links';

export default {
    init() {
        page__links();
    },
    finalize() {
        // JavaScript to be fired on the home page, after the init JS
    },
};
